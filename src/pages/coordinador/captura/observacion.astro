---
import LayoutCoordinador from '../../../layouts/LayoutCoordinador.astro';
const docenteIdParam = Astro.url.searchParams.get('docente_id');
const qs = docenteIdParam ? `?docente_id=${docenteIdParam}` : '';
---
<LayoutCoordinador titulo="Observación — Coordinador">
  <div class="animate-flotar-entrada max-w-2xl mx-auto">
    <h1 class="text-3xl font-bold text-tup mb-2">Nueva Observación</h1>
    <p class="text-slate-500 mb-8">Selecciona el tipo de observación que deseas realizar</p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <a href={`/coordinador/captura/observacion-escolarizado${qs}`} class="bg-white rounded-2xl p-6 shadow-tarjeta hover:shadow-tarjeta-hover hover:scale-105 transition-all text-center group">
        <div class="w-16 h-16 mx-auto rounded-2xl bg-tup/10 flex items-center justify-center mb-4 group-hover:bg-tup/20 transition-colors">
          <span class="text-3xl">🏫</span>
        </div>
        <h2 class="text-lg font-bold text-tup mb-2">Escolarizado</h2>
        <p class="text-sm text-slate-500">45 reactivos en 8 secciones. Evaluación completa de clase presencial.</p>
      </a>

      <a href={`/coordinador/captura/observacion-virtual${qs}`} class="bg-white rounded-2xl p-6 shadow-tarjeta hover:shadow-tarjeta-hover hover:scale-105 transition-all text-center group">
        <div class="w-16 h-16 mx-auto rounded-2xl bg-blue-100 flex items-center justify-center mb-4 group-hover:bg-blue-200 transition-colors">
          <span class="text-3xl">💻</span>
        </div>
        <h2 class="text-lg font-bold text-tup mb-2">Virtual</h2>
        <p class="text-sm text-slate-500">20 reactivos en 6 secciones. Evaluación de clase en línea.</p>
      </a>

      <a href={`/coordinador/captura/observacion-ejecutivo${qs}`} class="bg-white rounded-2xl p-6 shadow-tarjeta hover:shadow-tarjeta-hover hover:scale-105 transition-all text-center group">
        <div class="w-16 h-16 mx-auto rounded-2xl bg-amber-100 flex items-center justify-center mb-4 group-hover:bg-amber-200 transition-colors">
          <span class="text-3xl">💼</span>
        </div>
        <h2 class="text-lg font-bold text-tup mb-2">Ejecutivo</h2>
        <p class="text-sm text-slate-500">17 reactivos en 5 secciones. Evaluación de clase ejecutiva.</p>
      </a>
    </div>
  </div>
</LayoutCoordinador>
